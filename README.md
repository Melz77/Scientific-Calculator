# Scientific Calculator (TypeScript)

## Run locally

1. Install Node.js (>=14).
2. `npm install`
3. `npm run build`
4. `npm run start` and open http://localhost:8080

## Deploy

Push to GitHub and use the included GitHub Actions workflow to automatically build and publish `dist/` to the `gh-pages` branch.
